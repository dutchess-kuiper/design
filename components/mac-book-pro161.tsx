import type { NextPage } from "next";

const MacBookPro161: NextPage = () => {
  return (
    <input
      className="[border:none] font-medium font-quote-medium text-5xl bg-ghostwhite w-[1728px] h-[900px] overflow-hidden"
      value="Revenue Overview"
      placeholder="Dashboard"
      type="text"
      defaultValue="Revenue Overview"
    />
  );
};

export default MacBookPro161;
